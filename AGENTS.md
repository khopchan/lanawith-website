# LANA WITH. Codex運用ルール

このリポジトリはLANA WITH.公式ホームページです。

## 基本方針

- IT初心者の編集担当者が、日本語で画像変更・文章変更を依頼します。
- Codexは依頼内容を整理し、必要最小限のファイルだけを変更してください。
- 本番公開の最終承認は、必ず管理責任者が行います。
- Codexが独断で`main`へ直接反映してはいけません。

## 必須の作業手順

1. 作業開始前に、現在のブランチ、Git状態、未コミット変更を確認する。
2. 最新の`main`を基準に、依頼ごとの作業用ブランチを使用する。
3. 変更予定ファイル、作業内容、影響範囲を最初に報告する。
4. 不明点がある場合は推測して進めず、質問して停止する。
5. 依頼された範囲だけを変更する。
6. 変更後に`git diff`を確認する。
7. `npm run build`を実行する。
8. 可能な場合は`npm run lint`も実行する。
9. 作業用ブランチへコミットし、Pull Requestを作成する。
10. Vercel Previewで確認できる状態にする。
11. 管理責任者の承認前に`main`へマージしない。

## 画像変更ルール

- 公開用画像は原則として`public/images/`へ保存する。
- ファイル名は半角英数字とハイフンを使用する。
- 日本語、空白、`final`、`new2`のような曖昧な名前を避ける。
- 既存画像は依頼がない限り削除しない。
- 商品画像の参照は主に`app/page.tsx`内にある。
- 指定商品以外の画像参照を変更しない。
- 商品名、価格、説明文、容量、アレルギー情報、賞味期限、保存方法は、画像変更依頼だけでは変更しない。
- PC表示とスマートフォン表示の両方で、画像の見切れや不自然な拡大がないことを確認する。

## 文章変更ルール

- 指定された文章だけを変更する。
- 誤字修正であっても、周辺文章を独断で書き換えない。
- 商品名、価格、リンク、画像、レイアウトは、明示された場合だけ変更する。
- 同じ文章が複数箇所にある場合は、変更対象を一覧で報告してから進める。

## 価格変更ルール

- 正式な商品名、現在価格、変更後価格、税込・税別、対象サイズを確認する。
- 類似商品やミニサイズへ自動で変更を広げない。
- 同一価格が複数箇所にある場合は、対象箇所を報告する。

## 変更禁止事項

明示的な承認がない限り、以下を変更してはいけません。

- `main`への直接Push
- VercelのDomains設定
- Vercelの環境変数
- DNS、ネームサーバー、ドメイン設定
- GitHubリポジトリの公開範囲
- GitHubとVercelの連携
- 依頼に関係のないライブラリやバージョン
- 依頼に関係のないリファクタリング
- 商品表示順
- ヘッダー、フッター、ボタン、リンク
- APIキー、パスワード、秘密情報のコードへの記載

## Pull Requestに必ず記載する内容

- 変更対象
- 変更内容
- 変更したファイル
- 追加した画像ファイル
- 変更していないことを確認した項目
- `npm run build`の結果
- `npm run lint`の結果
- Vercel Preview URL
- 発注者・管理責任者が確認すべき点

## 完了条件

以下が揃うまで作業完了としません。

- 指定箇所だけが変更されている
- ビルドが成功している
- Pull Requestが作成されている
- Previewで確認可能になっている
- 管理責任者の承認待ちになっている
